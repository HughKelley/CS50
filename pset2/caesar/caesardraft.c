#include <stdio.h>
#include <cs50.h>
#include <stdlib.h>
#include <ctype.h>

int main(int argc, string argv[])
{

    //get code key

    // will be a string so use atoi to convert to integer

    //get text to encipher

    // encipher text

        //for each letter
        //if alphabetic
        //preserve case
        //shift by key using array index number plus key to get index number of new letter in alphabet array

    //print cipher text

    string k = argv[1];

    int k = atoi(argv[1]);

    for (int i = 0, n = strlen(p); i < n; i++)
{
    printf("%c", p[i]);
}


    /*

    isalpha
    isupper
    islower
    asci chart url:  http://www.asciichart.com/
    need to create an array for letters and search within it?
    needs to use %26 to prevent going beyond z in alphabet
    */

//////////////////////////////////////////////////////////////////
//this returns the letter c
char letter = 'a';

char letter2 = letter + 2;      //2 could be key instead.

printf("%c\n", letter2);
//////////////////////////////////////////////////////////////////


    char letter = 'Z';

    if (isupper(letter))
    {

        printf(letter)
    }


}